from django.apps import AppConfig


class UsersConfig(AppConfig):
    """App configuration for the users_app Django application."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'users_app'
